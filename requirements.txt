# Runtime backend — đủ để chạy API, teleop và xử lý dataset.
# Torch nằm ở requirements-train.txt: nặng, CI chỉ chạy test API nên không cần.

# Core
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic>=2.10.0
pydantic-settings>=2.7.0
python-dotenv>=1.0.0

# Simulation
mujoco>=3.2.0
numpy>=1.26.0
scipy>=1.14.0

# Vision / Video I/O
opencv-python-headless>=4.10.0
av>=13.0.0

# Database
sqlalchemy[asyncio]>=2.0.36
alembic>=1.14.0
aiosqlite>=0.20.0

# Auth
pyjwt>=2.10.0
bcrypt>=4.2.0
python-multipart>=0.0.20

# Dataset I/O
pyarrow>=18.0.0
pandas>=2.2.0

# Data versioning — con trỏ *.dvc và dvc.lock được commit, payload thì không
dvc>=3.56.0

# Dev tools
ruff>=0.8.0
mypy>=1.13.0
pytest>=8.0.0
pytest-asyncio>=0.24.0
httpx>=0.28.0
