# Runtime backend — đủ để chạy API, teleop và xử lý dataset.
# Torch nằm ở requirements-train.txt: nặng, CI chỉ chạy test API nên không cần.

# Core
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic>=2.10.0
pydantic-settings>=2.7.0
python-dotenv>=1.0.0

# Simulation
# robosuite 1.5.x still depends on MuJoCo APIs that changed in 3.9.
mujoco>=3.3.0,<3.9.0
numpy>=1.26.0,<2.0.0
scipy>=1.14.0
# src/sim/skillgen/compat.py works around the 1.4 -> 1.5 grip site rename and
# the move to a per-env RNG, so we are pinned to the 1.5 API shape.
robosuite>=1.5.0,<1.6.0
h5py>=3.11.0
# Pulled in by mujoco, but we import them directly: OpenGL.GL for renderer
# identification in src/sim/gpu.py, glfw for the teleop window in
# scripts/teleop_ui.py.
PyOpenGL>=3.1.7
glfw>=2.7.0

# Vision / Video I/O
opencv-python-headless>=4.10.0,<4.12.0
av>=13.0.0

# Database
sqlalchemy[asyncio]>=2.0.36
alembic>=1.14.0
aiosqlite>=0.20.0

# Auth
pyjwt>=2.10.0
bcrypt>=4.2.0
python-multipart>=0.0.20
# Fernet, for the per-user Weights & Biases key at rest. Present transitively
# today, but declared because losing it would silently take stored keys with it.
cryptography>=43.0.0

# Dataset I/O
pyarrow>=18.0.0
pandas>=2.2.0

# Data versioning — con trỏ *.dvc và dvc.lock được commit, payload thì không
dvc>=3.56.0

# Dev tools
ruff>=0.8.0
mypy>=1.13.0
pytest>=8.0.0
pytest-asyncio>=0.24.0
httpx>=0.28.0
