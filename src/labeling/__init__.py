"""Auto-labeling package.

Submodules are intentionally imported lazily by API handlers and scripts so the
main backend can start even when optional scripted-generation dependencies are
not installed yet.
"""
